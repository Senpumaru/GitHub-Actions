# This is first workflow to help you get started with Actions

name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print Greeting
        run: echo "Hello Action!"
      - name: Print Goodbye
        run: echo "Farewell!"
    
